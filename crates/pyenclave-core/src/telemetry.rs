//! Coleta stdout/stderr, wait/status, rusage (CPU, pico RSS) (skeleton).

use crate::spec::RunResult;

pub fn collect_output_and_wait() -> RunResult {
    // TODO: pipes p/ stdout/stderr, wait4, rusage
    RunResult::default()
}
